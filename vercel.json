{
  "version": 2,
  "builds": [
    { "src": "public/*", "use": "@vercel/static" },
    { "src": "api/keys.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/keys.js" },
    { "src": "/(.*)", "dest": "public/$1" }
  ]
}
